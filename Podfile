# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '9.2'
use_frameworks!

target 'CubeTimer' do
  # Pods for CubeTimer
  pod 'SwiftCharts', '~> 0.4'

  target 'CubeTimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CubeTimerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end